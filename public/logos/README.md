# Organization logos

Monochrome SVG marks for Experience / Education rows (CSS-masked for light, dark, and tonal themes).

Expected filenames (wired in `app/data.ts`):

Authoring: square `viewBox`, single black fill on transparent, symbol/mark only (~40px display). No baked backgrounds or multi-color locks.
